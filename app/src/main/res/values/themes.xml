<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Definición de tu tema -->
    <style name="Theme.Cancionero" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- ... otras configuraciones ... -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Configuración del ícono de hamburguesa -->
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="navigationViewStyle">@style/NavigationViewStyle</item>
    </style>

    <style name="Theme.Cancionero.Settings" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/blue_dark</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Barra de estado negra -->
        <item name="android:statusBarColor">@color/status_bar_gray</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Configuración de colores para switches -->
        <item name="colorSecondary">@color/blue</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="colorSurface">@color/surface_color</item>
        <item name="colorOnSurface">@android:color/white</item>

        <!-- Para switches específicamente -->
        <item name="android:colorControlActivated">@color/blue</item>
        <item name="android:colorControlNormal">@color/switch_track_disabled</item>
        <item name="android:colorControlHighlight">@color/blue_light</item>

        <!-- Estilo personalizado para switches -->
        <item name="switchStyle">@style/CustomSwitchStyle</item>
    </style>

    <!-- Estilo personalizado para switches -->
    <style name="CustomSwitchStyle" parent="Widget.MaterialComponents.CompoundButton.Switch">
        <item name="android:colorControlActivated">@color/blue</item>
        <item name="android:colorControlNormal">@color/switch_track_disabled</item>
        <item name="thumbTint">@color/switch_thumb_color</item>
        <item name="trackTint">@color/switch_track_color</item>
    </style>

    <style name="DialogTransparent" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- Estilo personalizado para el ícono de hamburguesa -->
    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item> <!-- Puedes ajustar el color según tus necesidades -->
    </style>

    <style name="NavigationViewStyle" parent="Widget.Design.NavigationView">
        <item name="android:textSize">18sp</item> <!-- Ajusta el tamaño del texto según tus preferencias -->
    </style>

</resources>
